# PDF processing modules

PDF operation functions are being migrated from `assets/js/app.js` incrementally.

The current migration keeps the production processing engine intact while establishing stable module boundaries for subsequent extraction.
